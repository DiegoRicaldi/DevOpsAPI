version: '3.8' 

services:
  app:
    build: .  
    ports:
      - '3000:3000'  
    environment:
      - NODE_ENV=development
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY} 
    volumes:
      - .:/app  
    command: npm start  

 